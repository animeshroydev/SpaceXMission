query DetailsQuery($id : ID!) {
    launch(id: $id) {
        details
        mission_name
        launch_site {
            site_name
        }
        launch_success
        rocket {
            rocket_name
        }
        launch_date_local
        links {
            flickr_images
        }
    }
}